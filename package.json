{
  "name": "unipaper-bridge",
  "version": "0.2.0",
  "description": "A privacy-preserving MCP bridge for scholarly metadata, bounded citation-network discovery, and user-controlled institutional library access.",
  "type": "module",
  "bin": {
    "unipaper-bridge": "dist/index.js"
  },
  "files": [
    "dist/",
    "skills/",
    ".codex-plugin/",
    ".mcp.json",
    "README.md",
    "LICENSE",
    "PRIVACY.md",
    "TERMS.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:local:node": "npm ci --prefix local/khu-auth-mcp && npm run build --prefix local/khu-auth-mcp && npm ci --prefix local/zotero-mcp && npm run build --prefix local/zotero-mcp",
    "build:portable": "npm ci --prefix local/khu-auth-helper-portable && npm run build --prefix local/khu-auth-helper-portable",
    "build:khu-helper": "node local/scripts/khu-helper.mjs build",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "setup:khu": "node local/scripts/khu-helper.mjs setup",
    "status:khu": "node local/scripts/khu-helper.mjs status",
    "remove:khu": "node local/scripts/khu-helper.mjs remove",
    "test": "vitest run",
    "test:khu-helper": "node local/scripts/khu-helper.mjs test",
    "test:khu-helper:mac": "swift run --package-path local/khu-auth-helper khu-auth-self-test",
    "test:watch": "vitest",
    "check": "npm run build && npm test",
    "check:local": "npm run build:khu-helper && npm run test:khu-helper"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "express-rate-limit": "^8.2.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.0.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
